apiVersion: v1
kind: Service
metadata:
  name: {{ include "chartwithtomcat.fullname" . }}
  labels:
    {{- include "chartwithtomcat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chartwithtomcat.selectorLabels" . | nindent 4 }}
