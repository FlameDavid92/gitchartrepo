apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chartwithtomcat.fullname" . }}-test-connection"
  labels:
    {{- include "chartwithtomcat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chartwithtomcat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
